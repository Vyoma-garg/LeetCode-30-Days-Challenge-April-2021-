class Solution:
    def fib(self, n: int) -> int:
        if n==0:
            sum_= 0
        if n==1:
            sum_=1
        if n>1: 
            sum_=self.fib(n-1) + self.fib(n-2)
        return sum_
        
